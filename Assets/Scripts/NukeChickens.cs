using System.Collections;
using UnityEngine;

public class NukeChickens : MonoBehaviour
{
    public GameObject vfx;
    private GameObject spawn;
    public Transform chicken;
    //private void Start()
    //{
    //    StartCoroutine(OnCollisionEnter());
    //}
    //IEnumerator OnCollisionEnter(Collision collisionInfo)
    //{
    //    if (collisionInfo.gameObject.name == "Free Racing Car")
    //    {
    //        yield return new WaitForSeconds(2);
    //        Destroy(gameObject);
    //        spawn = Instantiate(vfx, transform.position);

    //    }
    //}

}
